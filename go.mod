module ibc-frontrun-testbed

go 1.24.3

require (
// No external dependencies required - using only standard library
// All blockchain interactions are done via CLI commands to the locally built gaiad binary
)
